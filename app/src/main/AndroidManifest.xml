<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sourcey.materiallogindemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.PaymentDetails"></activity>
        <activity android:name=".activities.StudentsList" />
        <activity android:name=".activities.AddSchoolActivity" />
        <activity android:name=".activities.YearSelection" />
        <activity android:name=".activities.AdminOperations" />
        <activity android:name=".activities.NewStudentActivity" />
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.SignupActivity"
            android:theme="@style/AppTheme.Dark" />
    </application>

</manifest>